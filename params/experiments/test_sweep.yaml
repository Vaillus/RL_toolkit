program: Session.py
method: bayes
parameters:
  function_approximator_info.layers_info.sizes.0:
    min: 1
    max: 500
  function_approximator_info.layers_info.sizes.1:
    min: 1
    max: 500
  policy_estimator_info.layers_info.sizes.0:
    min: 1
    max: 500
  policy_estimator_info.layers_info.sizes.1:
    min: 1
    max: 500
  

metric:
  name: actor loss
  goal: minimize
command: 
  - ${env}
  - python3
  - ${program}